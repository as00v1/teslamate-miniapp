<!-- components/empty-state/empty-state.wxml - 空态组件 -->
<view class="empty">
  <view class="empty-icon">—</view>
  <view class="empty-text">{{text}}</view>
  <view class="empty-sub" wx:if="{{sub}}">{{sub}}</view>
</view>
